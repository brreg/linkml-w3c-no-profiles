name: Generate and publish

on:
  push:
    branches: [main]
    paths:
      - 'src/linkml/**'
      - 'src/assets/containers/Dockerfile.linkml'
      - 'src/assets/containers/Dockerfile.python'
      - 'src/assets/containers/requirements-python-test.txt'
      - 'src/templates/**'
      - 'mkdocs/**'

jobs:

  ensure-images:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
      - name: Logg inn på GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Cache linkml-local image
        id: cache-linkml
        uses: actions/cache@v5
        with:
          path: linkml-local.tar.zst
          key: linkml-local-${{ hashFiles('src/assets/containers/Dockerfile.linkml') }}
      - name: Sjekk om linkml-local finst i GHCR
        id: ghcr-linkml
        if: steps.cache-linkml.outputs.cache-hit == 'true'
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/linkml-local:${{ hashFiles('src/assets/containers/Dockerfile.linkml') }}
          podman pull "$IMAGE" 2>/dev/null && echo "exists=true" >> $GITHUB_OUTPUT || echo "exists=false" >> $GITHUB_OUTPUT
      - name: Bygg linkml-local image
        if: steps.cache-linkml.outputs.cache-hit != 'true'
        run: |
          make linkml-build-docker
          podman save localhost/linkml-local:latest | zstd -T0 > linkml-local.tar.zst
      - name: Last inn linkml-local frå cache
        if: steps.cache-linkml.outputs.cache-hit == 'true' && steps.ghcr-linkml.outputs.exists != 'true'
        run: podman load < <(zstd -d -c linkml-local.tar.zst)
      - name: Push linkml-local til GHCR
        if: steps.ghcr-linkml.outputs.exists != 'true'
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/linkml-local:${{ hashFiles('src/assets/containers/Dockerfile.linkml') }}
          podman tag localhost/linkml-local:latest "$IMAGE"
          podman push "$IMAGE"

      - name: Cache python-pytest image
        id: cache-python
        uses: actions/cache@v5
        with:
          path: python-pytest.tar.zst
          key: python-pytest-${{ hashFiles('src/assets/containers/Dockerfile.python', 'src/assets/containers/requirements-python-test.txt') }}
      - name: Sjekk om python-pytest finst i GHCR
        id: ghcr-python
        if: steps.cache-python.outputs.cache-hit == 'true'
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/python-pytest:${{ hashFiles('src/assets/containers/Dockerfile.python', 'src/assets/containers/requirements-python-test.txt') }}
          podman pull "$IMAGE" 2>/dev/null && echo "exists=true" >> $GITHUB_OUTPUT || echo "exists=false" >> $GITHUB_OUTPUT
      - name: Bygg python-pytest image
        if: steps.cache-python.outputs.cache-hit != 'true'
        run: |
          make python-build-docker
          podman save localhost/python-pytest:latest | zstd -T0 > python-pytest.tar.zst
      - name: Last inn python-pytest frå cache
        if: steps.cache-python.outputs.cache-hit == 'true' && steps.ghcr-python.outputs.exists != 'true'
        run: podman load < <(zstd -d -c python-pytest.tar.zst)
      - name: Push python-pytest til GHCR
        if: steps.ghcr-python.outputs.exists != 'true'
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/python-pytest:${{ hashFiles('src/assets/containers/Dockerfile.python', 'src/assets/containers/requirements-python-test.txt') }}
          podman tag localhost/python-pytest:latest "$IMAGE"
          podman push "$IMAGE"

  generate-ap-no:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (ap-no)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/ap-no/
          key: generated-ap-no-${{ hashFiles('src/linkml/ap-no/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ap-no
      - uses: actions/upload-artifact@v4
        with:
          name: generated-ap-no
          path: generated/ap-no/
          retention-days: 1

  generate-fair:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (fair)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/fair/
          key: generated-fair-${{ hashFiles('src/linkml/fair/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fair
      - uses: actions/upload-artifact@v4
        with:
          name: generated-fair
          path: generated/fair/
          retention-days: 1

  generate-fint:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (fint)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/fint/
          key: generated-fint-${{ hashFiles('src/linkml/fint/**', 'src/linkml/ap-no/**', 'src/linkml/fair/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: fint
      - uses: actions/upload-artifact@v4
        with:
          name: generated-fint
          path: generated/fint/
          retention-days: 1

  generate-ngr:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (ngr)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/ngr/
          key: generated-ngr-${{ hashFiles('src/linkml/ngr/**', 'src/linkml/ap-no/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: ngr
      - uses: actions/upload-artifact@v4
        with:
          name: generated-ngr
          path: generated/ngr/
          retention-days: 1

  generate-oreg:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (oreg)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/oreg/
          key: generated-oreg-${{ hashFiles('src/linkml/oreg/**', 'src/linkml/ap-no/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: oreg
      - uses: actions/upload-artifact@v4
        with:
          name: generated-oreg
          path: generated/oreg/
          retention-days: 1

  generate-samt:
    needs: ensure-images
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v6
      - name: Cache genererte artefaktar (samt)
        id: cache-generated
        uses: actions/cache@v5
        with:
          path: generated/samt/
          key: generated-samt-${{ hashFiles('src/linkml/samt/**', 'src/linkml/ap-no/**', 'src/assets/containers/Dockerfile.linkml', 'src/templates/**') }}
      - uses: ./.github/actions/load-images
        if: steps.cache-generated.outputs.cache-hit != 'true'
      - uses: ./.github/actions/gen-linkml
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-context
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-shapes
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-python
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-json-schema
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-owl
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-rdf
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-examples
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-doc
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: ./.github/actions/gen-erdiagram
        if: steps.cache-generated.outputs.cache-hit != 'true'
        with:
          domain: samt
      - uses: actions/upload-artifact@v4
        with:
          name: generated-samt
          path: generated/samt/
          retention-days: 1

  publish:
    needs: [generate-ap-no, generate-fair, generate-fint, generate-ngr, generate-oreg, generate-samt]
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v6

      - name: Last ned alle genererte artefaktar
        uses: actions/download-artifact@v4
        with:
          pattern: generated-*
          path: generated/
          merge-multiple: true

      - name: Cache mkdocs-local image
        id: cache-mkdocs
        uses: actions/cache@v5
        with:
          path: mkdocs-local.tar.zst
          key: mkdocs-local-${{ hashFiles('mkdocs/Dockerfile.mkdocs') }}
      - name: Bygg mkdocs-local image
        if: steps.cache-mkdocs.outputs.cache-hit != 'true'
        run: |
          make docs-build-docker
          podman save localhost/mkdocs-local:latest | zstd -T0 > mkdocs-local.tar.zst
      - name: Last inn mkdocs-local image
        run: podman load < <(zstd -d -c mkdocs-local.tar.zst)

      - name: Cache mkdocs-build-cache
        uses: actions/cache@v5
        with:
          path: mkdocs/.cache/
          key: mkdocs-build-${{ hashFiles('src/linkml/**', 'src/assets/containers/Dockerfile.linkml', 'mkdocs/mkdocs.yml', 'mkdocs/docs/**', 'src/templates/**') }}
          restore-keys: mkdocs-build-

      - name: Publiser og bygg dokumentasjonsportal
        run: |
          make publish
          make docs-build

      - name: Konfigurer GitHub Pages
        uses: actions/configure-pages@v6

      - name: Last opp Pages-artefakt
        uses: actions/upload-pages-artifact@v5
        with:
          path: mkdocs/site/

      - name: Deploy til GitHub Pages
        id: deploy
        uses: actions/deploy-pages@v5
